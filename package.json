{
  "name": "monorepo",
  "version": "1.0.0",
  "packageManager": "yarn@4.2.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspaces foreach --all run lint",
    "build": "yarn workspaces foreach --all run build",
    "jest": "yarn workspaces foreach --all run jest"
  },
  "devDependencies": {
    "@eslint/js": "^9.4.0",
    "eslint": "^9.4.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.11.0"
  },
  "private": true
}
